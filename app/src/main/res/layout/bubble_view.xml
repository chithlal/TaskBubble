<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:animateLayoutChanges="true"
    android:layout_height="wrap_content">

    <ImageView
        android:layout_width="@dimen/size_bubble_image"
        android:layout_height="@dimen/size_bubble_image"
        android:id="@+id/imageView_open_bubble"
        android:src="@drawable/ic_open_bubble"
        />
    <ImageView
        android:layout_width="@dimen/size_bubble_image"
        android:layout_height="@dimen/size_bubble_image"
        android:id="@+id/imageView_close_bubble"
        android:visibility="gone"
        android:src="@drawable/ic_rclose_bubble_24"
        />
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="@dimen/task_list_width"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:visibility="gone"
        android:layout_marginTop="@dimen/size_bubble_image"
        android:id="@+id/rv_task_list"
        />
</FrameLayout>